<h1>Editing Offer Product</h1>

<%= render 'form', offer_product: @offer_product %>

<%= link_to 'Show', "#{@offer_product}?#{@params}" %> |
<%= link_to 'Back', "#{offer_products_path}?#{@params}" %>
