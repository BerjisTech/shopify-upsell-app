<h1>Editing Auto Collection</h1>

<%= render 'form', auto_collection: @auto_collection %>

<%= link_to 'Show', "#{@auto_collection}?#{@params}" %> |
<%= link_to 'Back', "#{auto_collections_path}?#{@params}" %>
