<h1>Editing Condition Block</h1>

<%= render 'form', condition_block: @condition_block %>

<%= link_to 'Show', "#{@condition_block}?#{@params}" %> |
<%= link_to 'Back', "#{condition_blocks_path}?#{@params}" %>
