<div class="row">
  <div class="col-sm-3">
    <span>Create new offer</span>
  </div>
  <div class="col-sm-9">
    <%= render 'form', offer: @offer %>
  </div>
</div>

<% link_to 'Back', "#{offers_path}?#{@params}" %>
