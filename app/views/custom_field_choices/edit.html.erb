<h1>Editing Custom Field Choice</h1>

<%= render 'form', custom_field_choice: @custom_field_choice %>

<%= link_to 'Show', "#{@custom_field_choice}?#{@params}" %> |
<%= link_to 'Back', "#{custom_field_choices_path}?#{@params}" %>
