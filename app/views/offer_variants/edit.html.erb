<h1>Editing Offer Variant</h1>

<%= render 'form', offer_variant: @offer_variant %>

<%= link_to 'Show', "#{@offer_variant}?#{@params}" %> |
<%= link_to 'Back', "#{offer_variants_path}?#{@params}" %>
